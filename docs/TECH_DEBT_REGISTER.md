# Registro de Deuda Técnica

Mejoras detectadas que **no bloquean producción**. Si algo bloquea, va a
`PENDING_DECISIONS.md` o se corrige en el momento — esto es exclusivamente
para lo que puede esperar sin poner en riesgo un lanzamiento.

Cada entrada referencia el commit o módulo donde se detectó, para no
perder el contexto con el tiempo.

| Prioridad | Módulo | Impacto | Esfuerzo | Motivo |
|---|---|---|---|---|
| Baja | 2.2 Dashboard | Subestima levemente el % de ocupación cuando un bloqueo de ausencia se solapa solo parcialmente con una franja de disponibilidad (descuenta la franja completa, no la porción exacta) | Medio (aritmética de intersección de intervalos en SQL) | Documentado como simplificación V1 aceptable en `01-PRD/05_KPIs.md` — el caso típico (ausencia de día completo) ya es exacto |
| Baja | 2.4 Gestión de Staff | Documentos del Staff (certificados, contratos) no tienen pantalla ni storage — solo la arquitectura está prevista (bucket + tabla, sin tocar el resolutor de permisos) | Bajo cuando se necesite | Sin requisito concreto de qué documento guardar ni quién debe verlo todavía — construirlo ahora sería una tabla sin consumidor real |
| Media | 2.4 Gestión de Staff | La comisión de un Staff (`comision_pct`) se fija una sola vez al invitarlo y queda de solo lectura en su ficha — no hay forma de editarla después sin retirarlo y re-invitarlo | Bajo (una RPC `actualizar_comision_staff`, análoga a las ya existentes) | Pertenece conceptualmente a Finanzas (Módulo 2.10, no construido) — se prioriza cuando ese módulo se construya |
| Alta (antes de lanzamiento real) | Fase 1 — Legal | Los textos legales sembrados (Política de Datos, Términos v1) son un borrador funcional, no una revisión de un abogado colombiano | N/A — requiere revisión externa | Flaggeado desde el inicio del proyecto; no bloquea el desarrollo del producto, sí un lanzamiento comercial real |
| Media | 2.5 Servicios | `servicio_combo.precio_total_override`/`duracion_minutos_override` se guardan y se muestran en la ficha del combo, pero el motor de disponibilidad (`slots_disponibles`) sigue sumando la duración/precio de cada Servicio individual — el override todavía no tiene ningún efecto en una Reserva real | Medio (requiere detectar en `slots_disponibles` si el conjunto exacto de `servicio_ids` coincide con un combo configurado, y tocar ese RPC crítico) | `slots_disponibles` ya fue el origen de 2 bugs reales corregidos en sesiones anteriores (`max(uuid)`, `found` pisado) — se prioriza no arriesgar ese código verificado por una función opcional del combo |
| Baja | 2.9 Inventario | No existe una operación de "transferencia entre Sedes" (mover stock de la Sede A a la Sede B en un solo paso) — hay que registrar una SALIDA en una y una ENTRADA en la otra por separado | Bajo (una función que envuelva las dos llamadas ya existentes en una transacción) | Sin evidencia todavía de que sea un caso frecuente; se prioriza cuando un Negocio real lo pida — ver `ADR_009_Inventario_Stock_Por_Sede.md` |
| Media | 3.1 SuperSU | "Impersonación auditada" (SuperSU entra como si fuera el dueño de un Negocio, para dar soporte) mencionada en `02-UX/10_Super_Admin.md` no se construyó en este módulo | Medio-Alto (requiere un mecanismo de sesión temporal con auditoría obligatoria — no es una simple RPC, toca autenticación) | Es la funcionalidad de mayor riesgo de seguridad de toda la Fase 3 — se prioriza construirla con más tiempo dedicado exclusivamente a eso, probablemente junto con Soporte (3.3), en vez de apurarla dentro de 3.1 |
| Baja | 3.2 SuperSU | Créditos IA (`credito_ia_lote`), conversaciones WhatsApp (`whatsapp_conversacion_lote`) y Feature Flags (`feature_flag`) ya existen en el schema desde la migración 004, pero `02-UX/10_Super_Admin.md` los lista dentro de "Configuración global" sin ninguna UI para editarlos hoy | Bajo cuando exista el consumidor (una RPC de edición análoga a `actualizar_plan()`) | Ninguna de las tres tiene todavía un sistema real que la consuma (IA de Negocio y WhatsApp Engine son Fase 6) — construir su UI de configuración ahora sería una pantalla sin nada que configurar, contra la Regla de Oro |
| Baja | 3.2 SuperSU | "Crear" un Plan SaaS nuevo (quinto tramo) no se construyó — solo "editar" los 4 ya seedeados | Medio (requiere extender el enum `plan_codigo`, que hoy solo tiene RAVEN/JARL/VALHALLA/ALLFATHER) | La Biblia de Monetización no define un quinto tramo; extender el enum sin caso de uso real sería especulativo |
| Baja | 3.3 Soporte | Prioridades y SLA de ticket (mencionados en `02-UX/10_Super_Admin.md`) no se construyeron | Bajo (agregar una columna `prioridad` y una vista con temporizador) | La Biblia los nombra sin definir valores concretos (qué prioridades existen, cuántas horas de SLA por cuál) — construirlo ahora sería inventar una regla de negocio no escrita |
| Baja | 3.3 Soporte | Cliente (usuario del Marketplace, sin Negocio) no tiene todavía ninguna pantalla para crear/ver sus propios tickets, aunque la RLS y `actor_tipo_soporte()` ya lo permiten | Bajo (una pantalla en la app de Cliente) | Fuera del alcance de "SuperSU CMS" (Fase 3); se prioriza cuando se trabaje la experiencia de Cliente en el Marketplace (Fase 5) |

| Media | 4 App Staff | Bonos agregados de Puntualidad ("día 100% puntual" +5, "semana completa" +40) no se calculan — requieren un cierre periódico (día/semana) que agregue eventos de check-in, no solo el evento individual de llegada tarde | Medio (requiere un job programado — pg_cron o Edge Function — que no existe todavía en el proyecto) | Es el primer caso real de esta sesión que necesita infraestructura de cron; se prioriza junto con el rollover de Temporada (ver entrada siguiente), ambos comparten la misma necesidad de infraestructura |
| Media | 4 App Staff | Rollover automático de Temporada (cerrar la actual, consolidar Nivel, abrir la siguiente trimestral) no existe — la Temporada actual se sembró una vez en la migración 030 y se queda `EN_CURSO` indefinidamente | Medio-Alto (mismo job programado que el punto anterior) | Sin esto, ningún Staff pasará nunca a `nivel_staff_consolidado` ni empezará una Temporada nueva — es deuda real, no solo teórica, pero de infraestructura (cron), no de UI |
| Baja | 4 App Staff | "Cliente recurrente" y "Referido" (Calidad, +12/+20) no se implementaron | Medio (requiere definir qué cuenta como "recurrente" — no especificado con precisión en la Biblia) | Building esto ahora sería inventar una regla de negocio no escrita — se prioriza cuando se defina el criterio exacto |
| Baja | 4 App Staff | El bono "combo completo +18" de Producción no se otorga — solo se suman los puntos por Servicio individual | Bajo (mismo trabajo pendiente que el override de precio/duración de combos en `slots_disponibles`, ya registrado en 2.5) | Mismo motivo que la entrada de 2.5: requiere detectar si el conjunto exacto de Servicios de la Reserva coincide con un combo configurado |
| Baja | 4 App Staff | Agenda de la App Staff solo tiene vista de día, no de semana | Bajo (UI adicional sobre datos ya disponibles) | El caso de uso principal ("qué tengo que atender hoy") ya está cubierto; vista de semana se agrega si un Staff real la pide |
| Baja | 4 App Staff | No existe una pantalla "Inicio" separada (hoy/próximo cliente/objetivos) — Agenda ya cubre "hoy"/"próximo cliente" | Bajo | Los "objetivos" de Staff son ADR-008 (Fase 6, requiere IA); una pantalla de Inicio sin objetivos reales sería redundante con Agenda |

| Baja | 5.2 Marketplace | La segmentación de una campaña (`campana_publicitaria.segmentacion` jsonb — ciudad/categoría objetivo) no se verifica en `Patrocinio_normalizado`; solo se chequea que la campaña esté ACTIVA y con presupuesto | Bajo (agregar condiciones al `exists(...)` una vez exista el shape real) | El sistema de Ads (Fase 6) todavía no define ni usa ese jsonb — construir el matching ahora sería inventar un formato no probado |
| Media | 5.2 Marketplace | Calidad_de_Staff y Patrocinio_normalizado siempre calculan 0 hoy (ninguna Temporada cerró jamás — ADL-020; ningún sistema de Ads existe — Fase 6) | N/A, se resuelve solo cuando esas fases avancen | Documentado explícitamente en ADL-021/tasklist para que no se lea como un bug — son ceros honestos, no un cálculo fallido |
| Baja | 5.2 Marketplace | El "Mapa visual con pines" (MapLibre + OpenStreetMap) queda para el Módulo 5.3 — hoy la geolocalización real y el chip "Cerca de mí" ya afectan el ranking, pero no hay visualización de mapa | Medio (integrar MapLibre GL JS + tiles de OSM) | Es una superficie de UI separada del motor de ranking en sí, que ya está completo y verificado |

| Media | 6.2 Marketplace Ads | Formatos Banner y Promoción Flash no se exponen en la UI de creación | Medio-Alto (Banner necesita renderizado real en el carrusel del Home + creativo/imagen; Flash necesita el motor de Push, bloqueado — ver Decisión Pendiente de Firebase) | Exponer la creación sin que tuvieran ningún efecto real habría sido un botón muerto (Regla de Oro) |
| Media | 6.2 Marketplace Ads | La garantía posicional exclusiva del Pin patrocinado (siempre en las primeras posiciones) y el límite de saturación (2 posiciones consecutivas patrocinadas máximo) no están implementados — Pin usa hoy el mismo boost de Score que Destacado | Medio-Alto (toca de nuevo el `ORDER BY` de `marketplace_buscar()`, ya corregido 5 veces en el Módulo 5.2) | Se prioriza no arriesgar esa función crítica una sexta vez sin un módulo con foco exclusivo en este riesgo |
| Baja | 6.2 Marketplace Ads | `presupuesto_total` se hizo obligatorio en `crear_campana_publicitaria()` — la Biblia lo describe como opcional | Medio (requiere un job programado de recorte/reserva diaria contra el Wallet, sin infraestructura de cron en el proyecto todavía) | Mismo tipo de limitación que el rollover de Temporada (ADL-020/Fase 4) — documentado explícitamente, no un olvido |
| Baja | 6.2 Marketplace Ads | SuperSU puede pausar cualquier campaña a nivel de RPC (verificado), pero `/admin` no tiene un listado/selector de todas las campañas del proyecto para hacerlo desde la UI | Bajo (una pantalla de listado, análoga a `/admin/negocios`) | Sin evidencia todavía de que sea un caso de uso frecuente — se prioriza si SuperSU lo necesita en la práctica |

| Media | 6.3 Suscripciones | El calendario automático de reintentos de cobro (Día 0/1/3/7/10, `05_Billing_Failures.md`) no está construido — ver `PENDING_DECISIONS.md`, requiere Mercado Pago Preapproval + medio de pago guardado, integración distinta a Checkout Pro | Alto (nueva integración de pasarela + UI de "guardar tarjeta") | Genuino bloqueo de infraestructura de pagos, no una simplificación de alcance — sí se construyó el camino manual completo (marcar en mora, forzar reactivación por pago externo) que no depende de esto |
| Baja | 6.3 Suscripciones | Puntos de fidelización "congelados" durante suspensión (`03-Business-Rules/04_Lealtad.md`, caso límite) no tienen ningún efecto nuevo — no existe todavía ninguna RPC de canje de Puntos en el proyecto | Bajo cuando exista el canje | No hay nada que proteger todavía: construir el freeze antes que el canje mismo sería especulativo |
| Baja | 6.3 Suscripciones | El bono agregado de Puntualidad y el rollover de Temporada (Fase 4, ya registrados como deuda) podrían engancharse al mismo `/api/cron/diario` en vez de necesitar infraestructura propia — no se hizo en este módulo por disciplina de alcance | Bajo (ya existe el cron; solo agregar la llamada RPC correspondiente) | Se prioriza cuando se retome directamente ese módulo, para no tocar código de Fase 4 ya verificado sin necesidad |

| Baja | 6.5 Lealtad | Gift Cards físicas quedan solo como arquitectura de datos lista (el modelo ya soporta cualquier Gift Card sin cambios) — no hay flujo de emisión/impresión física ni proveedor integrado | Medio cuando exista un proveedor de impresión | Sin caso de uso concreto todavía; construir el flujo de impresión ahora sería especulativo |
| Media | 6.5 Lealtad | El uso de una Membresía (Servicio incluido/descuento) se registra manualmente en el punto de servicio (`registrar_uso_membresia`) — no está integrado dentro de la creación/precio de la Reserva (`slots_disponibles`) | Medio-Alto (toca el motor de disponibilidad crítico, ya con 2 bugs históricos corregidos) | Mismo criterio que `servicio_combo` (2.5): se prioriza no arriesgar esa función sin un módulo con foco exclusivo en esto |
| Media | 6.5 Lealtad | El motor antifraude solo detecta Canje duplicado y Abuso de referido — Dispositivo repetido, IP sospechosa y Múltiples cuentas quedan sin construir | Alto (requiere fingerprinting cliente-side + captura de IP en cada request, infraestructura nueva) | Los dos tipos construidos son los únicos verificables con los datos que el proyecto ya captura hoy — el resto requiere instrumentación nueva, no solo una RPC |
| Media | 6.5 Lealtad | El StylerWallet (saldo de Gift Cards/Referidos/Cashback/VIP) todavía no se puede gastar como método de pago dentro de una venta de POS — `canjear_lealtad_wallet()` existe (Cliente auto-servicio, debita su propio saldo) pero no está conectado a `completar_venta_pos()`, y generalizarla para que Staff la use en nombre de un Cliente presente requeriría una autorización nueva (el Wallet no tiene alcance de Negocio, así que "Staff de cualquier Negocio" no debería poder debitar el saldo de cualquier Cliente sin atarlo a una venta concreta) | Medio-Alto (diseño de autorización nuevo, no solo una RPC) | Encontrado en ADR-014, Fase B, al construir `redimir_gift_card_pos()` (que sí resuelve "canje únicamente desde POS" acreditando el Wallet) — gastar ese saldo en la MISMA venta queda como una mejora separada y deliberadamente no resuelta con un atajo inseguro |
| Baja | 6.5 Lealtad | Nemotron (respaldo de IA) está completamente conectado pero la cuenta de tokenrouter.com tiene $0.00 de crédito | Ninguno (solo recargar la cuenta) | Ver `docs/PENDING_DECISIONS.md` — no bloquea nada mientras OpenRouter siga funcionando |

| Media | 6.6 AI OS | `ai_modelo_config` tiene Ollama y Gemini seedeados con `activo=false` — no hay servidor Ollama local disponible en este entorno ni `GEMINI_API_KEY` configurada | Ninguno hasta que exista la credencial/infraestructura (el Cost Optimizer ya los activaría automáticamente al detectarlas) | Ver `docs/PENDING_DECISIONS.md` — el Cost Optimizer funciona hoy con OpenRouter + Nemotron |
| Media | 6.6 AI OS | El ROI de IA (`/panel/ia`) solo reporta créditos consumidos, costo estimado y consumo por categoría — las métricas de resultado (clientes recuperados, reservas generadas por IA, ventas atribuidas, tiempo ahorrado, ahorro operativo) pedidas en la extensión del ADR-012/013 no se calculan | Alto (requiere un mecanismo de trazabilidad que vincule cada sugerencia/acción de IA con un resultado de negocio posterior — no existe hoy en ninguna parte del proyecto) | Se prefirió reportar honestamente "no medible todavía" en la UI en vez de inventar una fórmula de atribución sin datos reales detrás — ver `docs/PENDING_DECISIONS.md` |
| Baja | 6.6 AI OS | Exportación de reportes de ROI de IA solo tiene CSV — PDF y Excel (pedidos en la extensión del ADR-012/013) no se implementaron | Medio (requiere elegir e integrar una librería nueva — el proyecto no tiene ninguna hoy) | Ver `docs/PENDING_DECISIONS.md` — sin librería elegida ni caso de uso urgente todavía |
| Alta | 6.6 AI OS | El ROI de IA (`ai_roi_snapshot`/`ai_conversion`, ADR-014 Fase C) solo mide atribución real vía el Motor de recompensas de Lealtad (Reserva de un Cliente con una sugerencia de IA confirmada en los 30 días previos) — "campaña creada/enviada/apertura/clic" pedidos en la extensión del ADR no se miden porque no existe ningún motor de entrega multicanal (email/WhatsApp) real en el proyecto | Alto (construir un motor de campañas real — envío, tracking de apertura/clic — es un módulo entero, no una tabla de tracking) | Se prefirió medir honestamente lo que sí tiene una fuente de datos real (Reserva/Venta atribuible) en vez de crear tablas de tracking sin ningún evento real que las alimente — ver ADL/PENDING_DECISIONS |
| Baja | 6.6 AI OS | `ai_roi_snapshot.tiempo_ahorrado_minutos_estimado` existe como columna pero siempre queda `null` — no hay ninguna metodología de estimación de tiempo ahorrado definida | Medio (requiere que el fundador defina una fórmula, ej. minutos promedio que tomaría redactar manualmente una sugerencia vs. generarla por IA) | Se dejó la columna nullable a propósito en vez de inventar un número — ver `docs/PENDING_DECISIONS.md` |
| Baja | 6.6 AI OS | El Prompt Builder (`/panel/ia`) es un editor de texto simple (nombre + categoría + contenido en un textarea) — no un constructor visual de variables (`{{variable}}` con selector) como sugiere el nombre "Builder" del ADR-013 | Medio (UI de edición de plantillas con variables tipadas) | La arquitectura de datos (`ai_prompt.variables` jsonb) ya soporta un constructor visual futuro sin cambios de esquema — se prioriza cuando haya más de un consumidor real de la Biblioteca |
| Baja | 6.6 AI OS | `otorgar_creditos_plan_mensual()` evita duplicar el otorgamiento del mes verificando "sin otorgamiento PLAN en los últimos 30 días" (ventana móvil), no "no otorgado en el mes calendario actual" | Bajo (cambiar la condición a `date_trunc('month', now())`) | Simplificación deliberada: con un cron diario, la diferencia entre "cada 30 días" y "cada mes calendario" no genera doble cobro ni corte de servicio — se documenta para no sorprender a quien lo audite después |

## Cómo agregar una entrada
Cuando detectes algo que valga la pena mejorar pero que no bloquea el
módulo que estás cerrando: agregalo acá con el mismo formato, en vez de
dejarlo solo mencionado en un comentario de código que se puede perder.
